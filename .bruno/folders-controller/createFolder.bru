meta {
  name: createFolder
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/dms/v1/folders
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Test",
    "parent": {
      "id": "{{root}}"
    }
  }
}

vars:post-response {
  folderId: res.body.id
}
